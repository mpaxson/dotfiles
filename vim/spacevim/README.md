# Offline 

After moving everything over to the host (`~/.SpaceVim`, `~/.SpaceVim.d/`, `~/.cache/vimfiles/`)
  run the command 
`ln -s "$HOME/.SpaceVim" "$HOME/.vim"`
also for root if you want to sudo vim
replace <user> with your users
`sudo ln -s "/home/<user>/.SpaceVim" "/root/.vim"`
